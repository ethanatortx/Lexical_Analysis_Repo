Hash table documentation

Includes
	
	<vector>
	<string>
	<utility>
	<iostream>
	<functional>
	"hash.h"
	"linked_list.h"

Template <class T>

	T = data type to hash

Member types:
	
	hash 		std::pair<T, std::string>
	bucket		linked_list<typname hash>

Member classes:
	
	class iterator
		hash table iterator, random access
	class const_iterator
		constant hash table iterator, random access

Member functions

	hash_table(std::size_t = 3)
		Default constructor
		Provide bucket constant; defaults to three

	hash_table(const hash_table&)
		Copy constructor

	hash_table& operator=(const hash_table&)
		Assign contents of hash table

	hash_table& operator+=(const hash_table&)
		Append all elements from other hash table and resize

	bool operator==(const hash_table&) const
		Compare another hash table to this
		Return true if they are identical

	bool operator!=(const hash_table&) const
		Compare another hash table to this
		Return true if they are different

	iterator begin()
		Return iterator pointing to beginning of list

	const_iterator begin() const
	const_iterator cbegin() const
		Return constant iterator pointing to beginning of list

	iterator end()
		Return iterator pointing to the end

	const_iterator end() const
	const_iterator cend() const
		Return constantant iterator pointing to the last term

	bool empty() const
		Return whether or not the hash table contains any elements

	std::size_t size() const
		Return count of active buckets
		(buckets with elements)

	void clear()
		Empty all buckets

	linked_list<T> operator[](int)
							 (iterator)
		Get list of terms in bucket

	linked_list<T> at(int)
					 (iterator)
		Get list of terms in a bucket

	int find(const T&)
	iterator find(const T&)
		Return the location of the first bucket containing the value

	const int find(const T&) const
	const const_iterator(const T&) const
		Constant find function

	void resize(std::size_t)
		Change bucket constant and resize for maximum efficiency

	void print() const
		Print the hash table to the console

	~hash_table()


Iterator member functions:
	
	difference_type			int
	value_type				linked_list<typename hash>
	pointer					linked_list<typename hash>*
	reference				linked_list<typename hash>&
	iterator_category		std::random_access_iterator_tag


	iterator& operator+=(int)
	iterator operator+(int)
	iterator& operator-=(int)
	iterator operator-(int)
	difference_type operator-(iterator&)
	value_type operator[](int) const
	int operator<(const iterator&) const
	int operator>(const iterator&) const

